import Config

# This config.exs belongs to the app_status library itself and is only
# used if you run/test this repo standalone. When you add app_status as
# a dependency of a host app, put the equivalent block in *that app's*
# config/config.exs instead, e.g.:
#
#   config :app_status,
#     app_name: :my_app,
#     endpoint: MyAppWeb.Endpoint,
#     extension: MyApp.StatusExtension
